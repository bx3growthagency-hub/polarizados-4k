# photos-raw/

22 fotos originales de cámara/celular, sin curar, copiadas desde el export de Google Drive del cliente. Pendiente antes de usarse en el sitio:

- Seleccionar las mejores 8-15 (instalación, fachada, detalle de acabado, vehículos terminados).
- Hay pares duplicados exactos: `IMG_1674` / `IMG_1674(1)`, `IMG_1690` / `IMG_1690(1)`, `IMG_2954` / `IMG_2954(1)`.
- Comprimir y convertir a WebP antes de servir en producción (los originales pesan 2.9–9.4 MB cada uno).
- Ninguna tiene nombre descriptivo — revisar visualmente para identificar cuáles corresponden a los casos de `src/data/projects.json`.

El material sin curar (PDFs de catálogo/manual y video de Autofest) vive en `references/` en la raíz del repo, no aquí.
