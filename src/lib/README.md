# lib/

Utilidades esperadas para Fase 1 (pendientes de construir):

- `whatsapp.ts` — construye el link `wa.me` con mensaje pre-cargado según página/servicio
- `analytics.ts` — helper de eventos (GA4/Meta Pixel). No activar hasta tener IDs reales del cliente; no afirmar que la medición ya está instalada si no lo está.
